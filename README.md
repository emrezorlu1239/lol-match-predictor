# LoL Match Predictor
Cinematic League of Legends match outcome predictor powered by LightGBM with 90% accuracy.

## Run
cd backend && pip install -r requirements.txt && python predict.py
npm install && npm run dev
